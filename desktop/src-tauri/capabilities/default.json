{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Zippy overlay — default webview capabilities",
  "windows": ["main"],
  "permissions": ["core:default", "global-shortcut:default"]
}
